"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
async function getCommandOutput(client, command, message) {
    const output = [];
    const fakeMessage = message || {
        author: {
            color: "#ff0000",
            flags: [],
            ID: "some-id",
            sessionID: "some-session-id",
            nickname: "Someone",
        },
        color: "#ff0000",
        date: new Date(),
        type: "text",
    };
    await client.processCommands({
        message: {
            ...fakeMessage,
            content: command,
        },
        send: (...messages) => {
            const message = messages.join(" ");
            output.push(message);
        },
        reply: (...messages) => {
            const message = `@${fakeMessage.author.nickname}${messages.join(" ")}`;
            output.push(message);
        },
    });
    return output.join("\n");
}
exports.default = getCommandOutput;
//# sourceMappingURL=testCommand.js.map